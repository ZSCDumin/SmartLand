<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.guotu.gt.mapper.PermissionRoleMapper">

    <select id="selectByCode" parameterType="Byte" resultType="com.guotu.gt.domain.PermissionRole">
        SELECT *
        FROM permission_role
        WHERE code =#{code}
    </select>

    <insert id="insert" parameterType="com.guotu.gt.domain.PermissionRole">
        INSERT INTO permission_role
        VALUES(#{code},
        #{name},
        #{description},
        #{flag})
    </insert>

    <delete id="deleteByCode" parameterType="Byte">
        DELETE FROM permission_role
        WHERE code =#{code}
    </delete>

    <update id="updateByCode" parameterType="com.guotu.gt.domain.PermissionRole">
        UPDATE permission_role
        SET name =#{name},
        description =#{description},
        flag =#{flag}
        WHERE code =#{code}
    </update>

</mapper>